document.addEventListener("DOMContentLoaded", () => {
    const title = document.querySelector('.ksstn-title');
    if (title) {
        // Add a class for the shine effect
        title.classList.add('ksstn-shine');
    }
});